[build]
  command = "npm run build"
  publish = "dist"

# Set these in Netlify → Site settings → Environment variables (before build):
# VITE_CONVEX_URL
# VITE_CLERK_PUBLISHABLE_KEY

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
